from typing import Protocol

class Uploader(Protocol):
    def send_complete_status(self, file_paths : list) -> bool: ...
    def upload_single_file(self, dest_path : str, file_path : str,) -> bool: ...
    def upload_multiple_files(self, dest_path : str, file_path_list : list[str]) -> bool: ...